<div class="navbar-fixed">
    <div class="fix-width">
        <div class="header">
            <nav class="navbar navbar-expand-md navbar-light">
                @await Html.PartialAsync("_Logo.cshtml")
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#NavbarMain" aria-controls="NavbarMain" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="NavbarMain">
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Home")">
                                <span class="p-1"><i class="fa fa-home"></i></span>
                                <span class="p-1">Home</span>
                            </a>
                        </li>
                        @if (!User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Authentication")">
                                <span class="p-1"><i class="fa fa-users"></i></span>
                                <span class="p-1">Authentication</span>
                            </a>
                        </li>
                        }

                    </ul>
                </div>
            </nav>
        </div>
    </div>
</div>